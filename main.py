import sys

from Config import config
from oneTimePad import oneTimePad

while True:
    encrypt, plaintext, key = config()

    if encrypt:
        # encrypt path
    
    else:
        # decrypt path